services:
  web:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - .:/app
      - staticfiles:/app/staticfiles
      - media:/app/media
    environment:
      - DEBUG=1
      - SECRET_KEY=dev-secret-key-change-in-production
      - ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
      - CSRF_TRUSTED_ORIGINS=http://localhost:8001
      - DATABASE_URL=postgres://horilla_user:horilla_pass@db:5435/horilla_db
      - DB_INIT_PASSWORD=dev-init-password-change-in-production
      - REDIS_URL=redis://:horilla_pass@redis:6380/0
      - CELERY_BROKER_URL=redis://:horilla_pass@redis:6380/0
      - CELERY_RESULT_BACKEND=redis://:horilla_pass@redis:6380/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/"]
      interval: 30s
      timeout: 30s
      start_period: 60s
      retries: 3

  celery-worker:
    build: .
    entrypoint: []
    command: celery -A horilla worker --loglevel=info --concurrency=2
    healthcheck:
      disable: true
    volumes:
      - .:/app
      - media:/app/media
    environment:
      - DATABASE_URL=postgres://horilla_user:horilla_pass@db:5432/horilla_db
      - REDIS_URL=redis://:horilla_pass@redis:6380/0
      - CELERY_BROKER_URL=redis://:horilla_pass@redis:6380/0
      - CELERY_RESULT_BACKEND=redis://:horilla_pass@redis:6380/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  celery-beat:
    build: .
    entrypoint: []
    command: celery -A horilla beat --loglevel=info
    healthcheck:
      disable: true
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=postgres://horilla_user:horilla_pass@db:5435/horilla_db
      - REDIS_URL=redis://:horilla_pass@redis:6380/0
      - CELERY_BROKER_URL=redis://:horilla_pass@redis:6380/0
      - CELERY_RESULT_BACKEND=redis://:horilla_pass@redis:6380/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    ports:
      - "5435:5432"
    environment:
      POSTGRES_DB: horilla_db
      POSTGRES_USER: horilla_user
      POSTGRES_PASSWORD: horilla_pass
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U horilla_user -d horilla_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    command: redis-server --appendonly yes --requirepass horilla_pass
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "horilla_pass", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - staticfiles:/static:ro
      - media:/media:ro
      - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      web:
        condition: service_healthy
    profiles: ["production"]

volumes:
  staticfiles:
  media:
  postgres_data:
  redis_data:
